# Continue (Sept 11)

- In-app recorder should stop at 60 seconds (see recording-context).
- `/capture` stores `aboutWhen` in localStorage (`pp.capture.draft`) for signup to pick up.
- `/demo` should render `DEMO_BEADS` through `ChronologyThread`.
- `/contribute` stays behind auth only. Do not link it from pricing or marketing.
- Magic link remains `/attorney/:token` and `/review/:token`.
- Print `https://pattern-proof.tech/intake`.
