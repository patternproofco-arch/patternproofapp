In `src/routes/demo.tsx`, replace the body of `function Timeline()` with:

```
import { TimelineTab } from "@/components/demo/TimelineTab";
function Timeline() {
  return <TimelineTab />;
}
```

Keep IncidentRow below for the journal tab.
