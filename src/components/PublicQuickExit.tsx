/**
 * Compatibility placeholder for pages that previously mounted their own
 * floating exit. The global header now owns the single persistent control.
 */
export function PublicQuickExit() {
  return null;
}
